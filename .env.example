# === ENV EXAMPLE (copiez en .env et remplissez) ===
# Ce fichier est un modèle. Ne mettez jamais vos vrais secrets dans Git.
# Après modification, redémarrez le serveur dev: `npm run dev`

# Optionnel: URL publique du site (utile pour liens absolus/emails)
# NEXT_PUBLIC_SITE_URL=https://www.smile-formation.fr

# ===============================================
# PARAMÈTRES FORMULAIRE DE CONTACT
# ===============================================
# Choisissez un provider: "brevo", "resend" ou "smtp" (ou laissez vide pour désactiver)
CONTACT_PROVIDER=brevo
# CONTACT_PROVIDER=resend
# CONTACT_PROVIDER=smtp

## Si vous utilisez Brevo (Sendinblue) pour stocker les leads
# Créez une clé API v3: https://app.brevo.com/settings/keys/api
# BREVO_API_KEY=xkeysib-********************************
# Optionnel: ID de la liste dans laquelle ajouter le contact (nombre)
# BREVO_LIST_ID=2

# Optionnel: mappage des attributs Brevo (utilisez les clés de votre compte)
# Créez au besoin les attributs côté Brevo: Contacts > Paramètres > Attributs
# Par défaut: FIRSTNAME/LASTNAME, SMS (téléphone), SUBJECT et MESSAGE (attributs texte personnalisés)
# Rétro-compatibilité: si vous utilisez un seul attribut pour le nom, définissez BREVO_ATTR_NAME
# BREVO_ATTR_FIRSTNAME=FIRSTNAME
# BREVO_ATTR_LASTNAME=LASTNAME
# BREVO_ATTR_NAME=FIRSTNAME
# BREVO_ATTR_PHONE=SMS
# BREVO_ATTR_SUBJECT=SUBJECT
# BREVO_ATTR_MESSAGE=MESSAGE

# Optionnel: si vous souhaitez aussi RECEVOIR un email à chaque soumission avec Brevo
# (le contact sera créé/MAJ ET un email transactionnel sera envoyé via Brevo si ces deux variables sont définies)
# CONTACT_TO_EMAIL=contact@smileformation.fr
# CONTACT_FROM_EMAIL="SMILE FORMATION <no-reply@votre-domaine.fr>"

## Si vous utilisez un provider email (Resend/SMTP), renseignez aussi:
# Adresse de destination (remplacez par l'email du client)
# CONTACT_TO_EMAIL=contact@votre-domaine.fr
# Adresse d'expéditeur affichée chez le destinataire
# CONTACT_FROM_EMAIL="SMILE FORMATION <no-reply@votre-domaine.fr>"

# --- RESEND (si CONTACT_PROVIDER=resend) ---
# Créez une clé API sur https://resend.com
# RESEND_API_KEY=re_********************************

# --- SMTP / Nodemailer (si CONTACT_PROVIDER=smtp) ---
# Renseignez les identifiants SMTP fournis par votre hébergeur
# SMTP_HOST=smtp.votre-fournisseur.fr
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=utilisateur_smtp
# SMTP_PASS=mot_de_passe_smtp

# Astuce:
# - Remplissez au minimum CONTACT_TO_EMAIL et CONTACT_FROM_EMAIL
# - Choisissez un provider (CONTACT_PROVIDER) et renseignez ses variables.
# - L’API de contact lit ces variables (voir app/api/contact/route.ts) quand vous l’activerez.
